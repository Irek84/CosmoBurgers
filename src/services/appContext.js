import React from 'react';

export const TotalPriceContext = React.createContext(0);
export const ConstructorContext = React.createContext(null);
export const OrderNumberContext = React.createContext(0); 